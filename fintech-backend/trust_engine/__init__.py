# =====================================================
# TRUST ENGINE — Statistical Trust Framework
# =====================================================
# Ships a Statistical Trust Certificate with every
# dataset Galarix generates. Proves:
#   1. Distribution fidelity (KS, chi-square, EMD)
#   2. Regional fidelity (correct scoring system, rates, currency)
#   3. Correlation fidelity (target vs actual)
#   4. Conditional compliance (zero violations)
#   5. Derived field accuracy (formula verification)
#   6. Full data provenance (source citations per region)
#
# This is what makes Galarix data TRUSTED, not just generated.
# =====================================================

from trust_engine.trust_report_builder import build_trust_certificate
